
{{- with .Values.udm }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "coran-udm.fullname" $ }}-service
  labels:
    {{- include "coran-udm.labels" $ | nindent 4 }}
    project: {{ $.Values.global.projectName }}
    nf: {{ .name }}
spec:
  type: {{ .service.type }}
  ports:
    - port: {{ .service.port }}
      targetPort: {{ .service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "coran-udm.selectorLabels" $ | nindent 4 }}
    project: {{ $.Values.global.projectName }}
    nf: {{ .name }}
{{- end }}
