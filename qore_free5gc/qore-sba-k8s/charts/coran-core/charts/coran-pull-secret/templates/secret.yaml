apiVersion: v1
kind: Secret
metadata:
  name: {{ (index $.Values.global.imagePullSecrets 0).name | quote }}
  annotations:
    "kubernetes.io/service-account.name": "default"
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ .Values.pullSecret.auth | b64enc }}
