apiVersion: v1
kind: Service
metadata:
  name: {{ include "busybox-alpine-chart.fullname" . }}
  labels:
    {{- include "busybox-alpine-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "busybox-alpine-chart.selectorLabels" . | nindent 4 }}
